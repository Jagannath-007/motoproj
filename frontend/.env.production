# AutoPulse CRM – Production Environment (Render)
# API is served from the same Express server — no separate VITE_API_URL needed
# VITE_API_URL is intentionally left blank (falls back to relative /api)
